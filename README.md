# Лабораторные работы №1–4: Платформа обработки задач

### Иващенко Юлия Васильевна
### Группа: М8О-102-БВ

Проект реализует подсистему приёма задач из различных источников (duck typing), доменную модель с валидацией, ленивую очередь с приоритетами и асинхронный исполнитель с расширяемыми обработчиками.

## Запуск проекта

Установка зависимостей:

```bash
pip install -r requirements.txt
```

Перед запуском добавьте корень репозитория в `PYTHONPATH`:

```bash
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
```

Демонстрация (лабы 1–4: приём задач → очередь → async-исполнитель):

```bash
python3 -m src.main
```

- **Логи**: пишутся в файл `app.log` и дублируются в консоль.

Тесты:

```bash
pytest
```

С покрытием:

```bash
pytest --cov
```

## Архитектура проекта

```text
.
├── src/
│   ├── common/
│   │   └── exeptions.py          # Пользовательские исключения (ValidationError и др.)
│   ├── models/
│   │   ├── contract.py           # TaskContract, AsyncHandlerContract (Protocol)
│   │   ├── descriptors.py        # Data-дескрипторы IntDescriptor, NonEmptyStr
│   │   ├── task.py               # Доменная модель Task (__add__, __radd__)
│   │   └── queue.py              # TaskQueue: куча, итерация, ленивые фильтры
│   ├── sources/
│   │   ├── api.py                # Источник: API-заглушка
│   │   ├── file.py               # Источник: JSON (задачи как dict -> Task)
│   │   └── generated.py          # Источник: программный генератор
│   ├── executor.py               # AsyncExecutor: asyncio.Queue, воркеры
│   ├── handlers.py               # UrgentTaskHandler, DefaultTaskHandler
│   ├── task_manager.py           # Менеджер источников и runtime-проверка контракта
│   └── main.py                   # Точка входа
├── tasks.json                    # Пример данных для FileSource
└── tests/
    ├── test_manager.py
    ├── test_models.py
    ├── test_queue.py
    ├── test_sources.py
    └── test_executor.py
```

---

## Реализованный функционал (по лабораторным)

### Лабораторная работа №1: Источники задач и контракты
Все источники задач (файлы, API, генераторы) не имеют общего базового класса, но реализуют единый поведенческий интерфейс с помощью `typing.Protocol` и `@runtime_checkable`. 
- **`TaskManager`** осуществляет строгую runtime-проверку объектов при добавлении.
- Источники возвращают данные итеративно (через `yield`), не перегружая оперативную память.

### Лабораторная работа №2: Модель задачи и дескрипторы
Доменная модель `Task` строго инкапсулирована:
- **Пользовательские дескрипторы**:
  - `IntDescriptor` — data-дескриптор, проверяющий тип и границы чисел (например, приоритет от 1 до 30).
  - `NonEmptyStr` — data-дескриптор, отсекающий пустые строки и пробелы (`strip`).
- **Свойства (`@property`)**: вычисляемые свойства `created_at` (только чтение) и `is_urgent` (приоритет > 10).
- При нарушении инвариантов генерируются кастомные ошибки из `src.common.exeptions` (например, `ValidationError`).

### Лабораторная работа №3: Очередь задач (Итераторы и генераторы)
В `src/queue.py` реализован класс пользовательской коллекции `TaskQueue`:
1. **Собственная структура данных (Priority Queue)**: 
   Вместо использования стандартного модуля `heapq`, внутри очереди реализована собственная куча (методы `_heapify_up`, `_heapify_down`, `_heap_push`, `_heap_pop`). Алгоритм Min-Heap адаптирован под Max-Heap через инверсию приоритетов.
2. **FIFO при равных приоритетах**: 
   Для задач с одинаковым приоритетом реализован строгий порядок FIFO (кто первый пришел, тот первый ушел) с помощью счетчика `itertools.count()`. Это также предотвращает ошибку сравнения объектов `Task` между собой.
3. **Ленивая обработка**: 
   Очередь читает источники и отдает данные лениво через `yield`. Реализованы методы потоковой фильтрации (`filter_by_status`, `filter_by_priority`), которые сохраняют сортировку по приоритету и не создают промежуточных списков в памяти.
4. **Поддержка стандартных конструкций Python**: 
   Очередь поддерживает многократный обход циклом `for`, конвертацию в `list()` и корректно обрабатывает `StopIteration`.
5. **Магические методы сложения**: 
   Модель `Task` реализует методы `__add__` и `__radd__`, что позволяет применять стандартную функцию Python `sum(queue)` ко всей очереди задач. Результатом является единичная задача с объединенным описанием и максимальным приоритетом.

### Лабораторная работа №4: Асинхронный исполнитель задач
В `src/executor.py` и `src/handlers.py` реализован асинхронный исполнитель с расширяемыми обработчиками:
1. **Асинхронная очередь**: 
   Задачи передаются через `asyncio.Queue`. Воркеры забирают их через `await queue.get()` и не блокируют event loop синхронными операциями (имитация работы — `asyncio.sleep`).
2. **Контракт обработчиков**: 
   Интерфейс задан протоколом `AsyncHandlerContract` в `src/models/contract.py` (`can_handle`, `handle`). Обработчики не наследуют общий базовый класс, а подключаются через `add_handler()`.
3. **Контекстный менеджер**: 
   `AsyncExecutor` поддерживает `async with`: при входе запускается пул воркеров (`asyncio.create_task`), при выходе дожидается завершения очереди (`queue.join()`) и корректно останавливает воркеры.
4. **Логирование и обработка ошибок**: 
   События исполнителя и обработчиков пишутся через `logging` (в `main.py` настроен вывод в `app.log` и консоль). Ошибки в воркере перехватываются, логируются и не роняют весь пул.
5. **Маршрутизация задач**: 
   В `handlers.py` реализованы `UrgentTaskHandler` (срочные задачи, `is_urgent`) и `DefaultTaskHandler` (остальные). Первый подходящий обработчик выполняет `handle`. Демонстрация в `main.py`: `TaskQueue` передаёт задачи в `asyncio.run(async_main(queue))`.
